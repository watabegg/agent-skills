interface:
  display_name: "Herdr Dev Loop"
  short_description: "Herdr上で実装・Gap検査・レビュー・有界収束・相談役をagent backend profile付きで運用する"
  default_prompt: "Use $herdr-dev-loop 0.5.2 with one explicit namespace to initialize and operate a bounded, profile-driven Herdr-managed Manager/Worker/Gap Auditor/Reviewer loop. Ignore legacy .ai/loop. Before other work, announce runtime version, namespace, loop version, and run_id; lock release scope before dispatch, use batch review cadence and explicit manual-final certification for new loops, and use Advisor only when explicitly requested."
